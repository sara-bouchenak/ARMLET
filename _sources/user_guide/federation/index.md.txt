(federation_mode)=

# Federation mode

The federation mode of **ARMLET** serves to run federated learning experiments.
This mode is chosen by default when running the `armlet` command.
On the following subpages, we explain how to use this mode and provide details on all the related configurations.

```{eval-rst}

.. toctree::
    :maxdepth: 2
    :hidden:

    run
    config/index

```
