# @package _global_
defaults:
  - /experiment/examples/benchmark/default.yaml
  - override /data/dataset: tabular/ars.yaml

data:
  seed: 59

method:

  hyperparameters:

    server:
      time_to_accuracy_target: 0.95

    client:
      batch_size: 1024
