# @package _global_
defaults:
  - /experiment/examples/benchmark/default.yaml
  - /data/cleaning: remove_mv.yaml
  - override /data/dataset: tabular/kdd.yaml

data:
  seed: 59

method:

  hyperparameters:

    server:
      time_to_accuracy_target: 0.9

    client:
      batch_size: 128
