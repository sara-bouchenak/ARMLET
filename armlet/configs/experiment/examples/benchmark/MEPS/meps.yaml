# @package _global_
defaults:
  - /experiment/examples/benchmark/default.yaml
  - override /data/dataset: tabular/meps.yaml

data:
  seed: 38

method:

  hyperparameters:

    server:
      time_to_accuracy_target: 0.85

    client:
      batch_size: 128
