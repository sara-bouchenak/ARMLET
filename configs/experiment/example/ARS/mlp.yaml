# @package _global_
defaults:
  - /experiment/example/ARS/ars.yaml
  - override /method/hyperparameters/model: mlp.yaml

method:
  hyperparameters:
    client:
      optimizer:
        name: Adagrad
        lr: 0.01
        weight_decay: 0

hydra:
  sweeper:
    params:
      exp.seed: 101,202,303,404,505
