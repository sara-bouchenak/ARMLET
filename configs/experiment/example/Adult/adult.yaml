# @package _global_
defaults:
  - /experiment/example/default.yaml
  - override /data/dataset: adult.yaml

data:
  seed: 59

method:

  hyperparameters:

    server:
      time_to_accuracy_target: 0.8

    client:
      batch_size: 128
