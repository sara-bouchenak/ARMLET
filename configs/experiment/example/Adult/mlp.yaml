# @package _global_
defaults:
  - /experiment/example/Adult/adult.yaml
  - override /method/hyperparameters/model: mlp.yaml

method:
  hyperparameters:
    client:
      optimizer:
        name: RMSprop
        lr: 0.0001
        weight_decay: 0.01

hydra:
  sweeper:
    params:
      exp.seed: 101,202,303,404,505
