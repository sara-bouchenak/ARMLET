# @package _global_
defaults:
  - /experiment/example/KDD/kdd.yaml
  - override /method/hyperparameters/model: log_regression.yaml

method:
  hyperparameters:
    client:
      optimizer:
        name: SGD
        lr: 0.01
        weight_decay: 0

hydra:
  sweeper:
    params:
      exp.seed: 101,202,303,404,505
