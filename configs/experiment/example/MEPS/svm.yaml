# @package _global_
defaults:
  - /experiment/example/MEPS/meps.yaml
  - override /method/hyperparameters/model: svm.yaml

method:
  hyperparameters:
    client:
      optimizer:
        name: RMSprop
        lr: 0.0003
        weight_decay: 0.1

hydra:
  sweeper:
    params:
      exp.seed: 101,202,303,404,505
