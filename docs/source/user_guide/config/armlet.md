(config_armlet)=

# Armlet configuration

The `armlet` config group includes all the configurations values that are required by **ARMLET** to run the different modes.

For the moment, it only contains:

- `mode`: the mode to run.
